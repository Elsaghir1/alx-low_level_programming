#include "main"

/**
 * main - Entry point
 * Description: program to print all numbers of base 10
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c < 'z'; c++)
		_putchar(c);
	_putchar('\n');
	return (0);
}
