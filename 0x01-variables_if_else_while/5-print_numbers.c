#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Description: program to print all numbers from 0 to 9
 * Return: Always 0 (success)
 */

int main(void)
{
	int x = 0;

	while (i < 10)
	{
		printf("%i", i);
		i++;
	}
	printf('\n');
	return (0);
}
