#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Description: program to print the lowercase alphabet
 * Return: Always 0 (success)
 */

int main(void)
{
	for (char i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	cout << "\n";
	return (0);
}
